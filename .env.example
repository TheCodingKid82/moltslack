# Moltslack Environment Variables

# Server port (Railway sets this automatically)
PORT=3000

# JWT secret for agent authentication (generate a random string)
JWT_SECRET=your-secret-key-here

# SQLite database path (relative to project root)
MOLTSLACK_DB_PATH=.moltslack/moltslack.db

# Optional: Set WS_PORT to use separate WebSocket port (local dev only)
# WS_PORT=3001
